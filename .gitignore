# Composer
/vendor/

# Node (if a build step is added later)
/node_modules/

# @wordpress/env working directory (when WP_ENV_HOME points inside the project)
/.wp-env-home/
/.wp-env-override.json

# Tooling caches
/.phpunit.cache/
/.phpunit.cache.integration/
/.phpcs.cache
/.phpstan.cache/
.phpunit.result.cache

# Build / distribution artifacts
/build/
/dist/
*.zip

# Local overrides of the shared config templates
phpcs.xml
phpstan.neon
phpunit.xml

# Editor / OS noise
/.idea/
/.vscode/
.DS_Store
Thumbs.db
