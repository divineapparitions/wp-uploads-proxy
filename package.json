{
	"name": "uploads-proxy",
	"private": true,
	"description": "Tooling for the Uploads Proxy WordPress plugin (wp-env integration test harness).",
	"license": "GPL-2.0-or-later",
	"scripts": {
		"env:start": "wp-env start",
		"env:stop": "wp-env stop",
		"test:integration": "wp-env run tests-cli --env-cwd=wp-content/plugins/wp-uploads-proxy vendor/bin/phpunit -c phpunit-integration.xml.dist"
	},
	"devDependencies": {
		"@wordpress/env": "^10.0.0"
	}
}
